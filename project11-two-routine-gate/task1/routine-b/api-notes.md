# Routine B API Notes

Trigger: API request

Authentication: Bearer token

The bearer token must be stored securely when first displayed.

Never commit the token to the repository.
Never place the token in transcripts, README, progress.md, or other tracked files.
